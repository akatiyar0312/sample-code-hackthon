<configuration>
  <!-- Console appender for local development -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Google Cloud Logging appender -->
  <appender name="CLOUD" class="com.google.cloud.logging.logback.LoggingAppender">
    <!-- Filter logs at INFO level and above -->
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>INFO</level>
    </filter>
    
    <!-- Custom log name (optional) -->
    <log>payment-backend</log>
    
    <!-- Resource type for Cloud Run -->
    <resourceType>cloud_run_revision</resourceType>
    
    <!-- Flush immediately for WARN and above -->
    <flushLevel>WARN</flushLevel>
    
    <!-- Include source location (helpful for debugging) -->
    <includeLocation>true</includeLocation>
    
    <!-- Include MDC (Mapped Diagnostic Context) -->
    <includeMdc>true</includeMdc>
  </appender>

  <!-- Profile-specific configuration -->
  <springProfile name="!prod">
    <!-- Use console logging for non-production -->
    <root level="DEBUG">
      <appender-ref ref="CONSOLE" />
    </root>
  </springProfile>

  <springProfile name="prod">
    <!-- Use Cloud Logging for production -->
    <root level="INFO">
      <appender-ref ref="CLOUD" />
    </root>
  </springProfile>

  <!-- Fallback if no profile is specified -->
  <root level="INFO">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="CLOUD" />
  </root>
</configuration>